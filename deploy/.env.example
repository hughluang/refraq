# Example env for deploy/compose.yaml (optional overrides).
# REFRAQ_API_UPSTREAM for the web image is a Dockerfile build-arg (http://api:8000), not a runtime env.

INITIAL_ADMIN_ACCOUNT=root
INITIAL_ADMIN_PASSWORD=change-me
ADMIN_SESSION_SECRET=change-me-in-production
